# B1: httpレベルの基本設定。/etc/nginx/conf.d/10-tare.conf として配置する（初回baselineの後）。
# nginx.confに同じdirectiveがある場合は重複エラーになるので、nginx -tで確認してから入れる。
sendfile on;
tcp_nopush on;
tcp_nodelay on;
keepalive_requests 10000;
